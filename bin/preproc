#!/usr/bin/perl
# vim: ts=2 sw=2 ft=perl
eval 'exec perl -x -wS $0 ${1+"$@"}'
  if 0;
$|++;
use common::sense;
use Time::HiRes qw(alarm);
use autodie;
use Nobody::Util;
use Nobody::PP;
our(@VERSION) = qw( 0 1 0 );
use XML::LibXML::Simple;

#    {
#      package TextMsg;
#      use Nobody::Util;
#      use common::sense;
#      use ParseXML;
#      sub parse {
#        my($text);
#        for($text=shift) {
#          s{^\s*<}{<} or last;
#          $text=path($_)->slurp;
#        };
#        $DB::single=1;
#        my($msg)=ParseXML->parse($text);
#        $msg;
#      };
#    };
#    our(@ISA)=qw(TextMsg::ListOf);
#    use IO::File;
#    for([sort glob("xml/*.xml")]->[0]) {
#      my($data)=TextMsg::parse($_);
#      eex($data);
#    }
#    #    for(@res) {
#    #      say map { $_->{date} } values %$_;
#    #    };
#    #    open(my $out,">out");
#    #    $out->say(pp(\@res));
#    #    close($out);
